# WireWAN Backend Configuration
# This file is auto-generated. Keep it secure and do not commit to version control.

# Encryption key for sensitive data (passwords, private keys)
# WARNING: Changing this will invalidate all previously encrypted data
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=wirewan-dev-key-change-in-production

# Salt for encryption key derivation - must be unique per installation
# WARNING: Changing this will invalidate all previously encrypted data
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(16))"
ENCRYPTION_SALT=wirewan-dev-salt-change-in-production

# Secret key for JWT tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=wirewan-jwt-secret-change-in-production
